# Example luna agents

Not installed. Grok only auto-loads `agents/` at the plugin root.

Copy a file into `agents/` (this plugin) or `~/.grok/agents/` to register it.

| File | Type | Effort |
|---|---|---|
| `openai-auth-fast.md` | `openai-auth-fast` | low |
| `openai-auth-researcher.md` | `openai-auth-researcher` | high |
| `openai-auth-implementer.md` | `openai-auth-implementer` | xhigh |

The plugin already ships `agents/openai-auth.md` (medium). How to spawn: `skills/chatgpt-oauth/SKILL.md`.
